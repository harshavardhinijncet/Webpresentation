# technicalhub.io — presentation portal
# Save it as `.env` either at the repository root (beside CLAUDE.md) or inside
# `backend/` — both are read, and `backend/.env` wins if a key is in both.
# Better still, paste these into the hosting panel's environment panel, and then
# they are never written to disk at all.
#
# It is already in .gitignore, so it cannot be committed by accident.
#
# There are no AWS credentials in this file. That is deliberate, not an
# omission: the application never talks to AWS. See the note at the bottom.

# --- network ----------------------------------------------------------------
PORT=4173

# Must be 0.0.0.0, not 127.0.0.1. On 127.0.0.1 the process only accepts
# connections from inside its own container and the reverse proxy gets refused.
HOST=0.0.0.0

# --- sign-in ----------------------------------------------------------------
# CHANGE BOTH PASSWORDS BEFORE THE FIRST BOOT.
# The defaults (Admin@123 / Present@123) are printed in the repository README
# and in every commit of it. On a public domain they are not a login.
# These seed the two accounts the first time the app starts.
ADMIN_EMAIL=admin@technicalhub.io
ADMIN_PASSWORD=CHANGE_ME_admin

PRESENTER_EMAIL=presenter@technicalhub.io
PRESENTER_PASSWORD=CHANGE_ME_presenter

# How long a sign-in lasts, in hours.
SESSION_TTL_HOURS=12

# Hides the "admin@org.local / Admin@123" hint under the login form.
# Must be 0 in production — it currently prints working credentials on a
# public page.
SHOW_LOGIN_HINT=0

# 1 once the domain serves HTTPS, so the session cookie carries `Secure`.
# Keep 0 while the site is reached over plain http at an IP address — a Secure
# cookie is dropped by the browser on http and nobody can sign in at all.
# Switch to 1 in the same change that puts TLS in front of it.
COOKIE_SECURE=0

# --- media ------------------------------------------------------------------
# LEAVE THIS EMPTY FOR NOW.
#
# Empty, the app serves images and films from its own disk (backend/uploads),
# which is what works today. Set, every media URL is prefixed with it and the
# files are fetched from the CDN instead.
#
# All 341 media files are already in the S3 bucket
# (s3://babji-neelam-727596873106-ap-south-2-an/uploads/), but the objects are
# private and there is no CloudFront distribution in front of them, so they
# answer 403 to a browser. Setting this before that is fixed breaks every image
# and every video on the deck at once.
#
# When CloudFront is set up, set it to the distribution root with no trailing
# slash and no /uploads on the end:
#   MEDIA_BASE_URL=https://cdn.technicalhub.io
MEDIA_BASE_URL=

# --- uploads ----------------------------------------------------------------
# Ceilings for admin uploads through the UI. Defaults are 25 and 200.
MAX_UPLOAD_MB=25
MAX_VIDEO_MB=200

# Optional. Only needed if video posters should be generated on upload; the
# app works without it.
# FFMPEG_PATH=/usr/bin/ffmpeg


# ============================================================================
# Notes for the deployment team
# ============================================================================
#
# 1. NO AWS KEYS. The application does not read AWS_ACCESS_KEY_ID or
#    AWS_SECRET_ACCESS_KEY and does not have an S3 client. Media is either
#    served from local disk or fetched by the browser over plain HTTPS from the
#    CDN. Uploading new media is `aws s3 sync` run by a person, not something
#    the app does — so it needs no credentials of its own, and there is nothing
#    in the running service to leak.
#
#    The key that was shared earlier (AKIA2S2BHGWJNORXX6SQ) should be deleted
#    in IAM. It is not in this repository or its history, but it was sent over
#    chat and must be treated as compromised.
#
# 2. THE CURRENT DEPLOYMENT AT 18.61.154.130:4173 IS OUT OF DATE and must be
#    redeployed from commit e84c518 or later. That build predates the
#    MEDIA_BASE_URL support, so it has no /config.js route — the MEDIA_BASE_URL
#    setting above will be ignored by it entirely.
#
# 3. START COMMAND: `node backend/src/server.js` from the repository root, or
#    `cd backend && node src/server.js`. Node 20 or newer.
#    There is no build step and no `npm install` — zero runtime dependencies.
#    If a build command is required by the platform, leave it blank.
#
# 4. HEALTH CHECK: GET /api/health returns {"ok":true,"build":"..."}.
#    Please also hit it every 10 minutes from a cron or an uptime monitor. On a
#    sleeping instance the first visitor otherwise waits ~50 seconds on a blank
#    screen and assumes the site is broken.
#
# 5. DATA IS A FILE. backend/data/db.json holds all the content. If the host
#    has no persistent disk, every restart resets it to whatever is in the
#    repository — anything an admin changes through the UI is lost. Content is
#    authored by committing, so this is survivable, but it must be known.
#    If a persistent volume is available, mount it over backend/data and
#    backend/uploads.
#
# 6. SECURITY HEADERS belong at the proxy or CloudFront, not in the app:
#    Strict-Transport-Security, X-Content-Type-Options: nosniff,
#    Referrer-Policy, and a CSP. If a CSP is added it needs
#    frame-src https://www.youtube-nocookie.com or every film on the deck
#    stops playing.
#
# 7. There is no SESSION_SECRET. Sessions are random tokens stored in db.json
#    rather than signed cookies, so there is nothing to sign. If you have seen
#    that variable in an earlier draft of the handover notes, it was a mistake
#    and has been corrected.
